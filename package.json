{
  "name": "dbschema-inator-top",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "lerna run start --stream",
    "clean": "lerna run clean --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "testintegration": "lerna run testintegration --stream",
    "integration": "lerna run integration --stream",
    "watch": "lerna run watch --stream"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.4.0",
    "typescript": "^3.2.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
